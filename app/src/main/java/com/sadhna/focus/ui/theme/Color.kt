package com.sadhna.focus.ui.theme

import androidx.compose.ui.graphics.Color

// ── Background & Surface ───────────────────────────────────────────────────
val BgDark      = Color(0xFF08070C)
val Surface1    = Color(0xFF111018)
val Surface2    = Color(0xFF1A1826)
val Surface3    = Color(0xFF231F35)
val Border      = Color(0x11FFFFFF)

// ── Brand — Saffron Fire ───────────────────────────────────────────────────
val FireOrange  = Color(0xFFFF6B00)
val FireGold    = Color(0xFFFFD000)
val FireLight   = Color(0xFFFFE066)

// ── Semantic ───────────────────────────────────────────────────────────────
val Productive  = Color(0xFF34D399)   // green  — productive app time
val Distracting = Color(0xFFF87171)   // red    — distracting app time
val InfoBlue    = Color(0xFF60A5FA)   // blue   — schedule info
val PurpleTag   = Color(0xFFA78BFA)  // purple — revision tag

// ── Text ───────────────────────────────────────────────────────────────────
val TextPrimary = Color(0xFFF0EDE8)
val TextMuted   = Color(0xFF7C7A8A)
val TextOnFire  = Color(0xFFFFFFFF)
